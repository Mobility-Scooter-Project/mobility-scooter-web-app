import { cn } from "~/lib/utils";

export function Sidebar() {
  return (
    <nav
      className={cn(
        "relative h-full w-12 rounded-full bg-card p-4"
      )}
    >
      {/* empty for now */}
    </nav>
  );
}