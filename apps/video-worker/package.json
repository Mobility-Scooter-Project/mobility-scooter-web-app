{
    "name": "@mswa/video-worker",
    "version": "1.0.0",
    "description": "Video processing worker for the video processing service",
    "scripts": {
        "dev": "nodemon --exec 'celery -A main worker -l INFO' --ext py --watch src"
    },
    "devDependencies": {
        "nodemon": "^3.1.9"
    },
    "dependencies": {
        "pino-pretty": "^13.0.0"
    }
}