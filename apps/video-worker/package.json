{
    "name": "@mswa/video-worker",
    "version": "1.0.0",
    "description": "Video processing worker for the video processing service",
    "scripts": {
        "dev": "nodemon --watch ./src --ext py --ignore 'src/**/*.pyc' --ignore 'src/**/__pycache__' --exec 'poetry run python -B src/main.py'"
    },
    "devDependencies": {
        "nodemon": "^3.1.9"
    },
    "dependencies": {
        "pino-pretty": "^13.0.0"
    }
}
