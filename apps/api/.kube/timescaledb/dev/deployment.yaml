apiVersion: apps/v1
kind: Deployment
metadata:
  name: mswa-timescaledb-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mswa-timescaledb-dev
  template:
    metadata:
      labels:
        app: mswa-timescaledb-dev
    spec:
      containers:
      - name: timescaledb
        image: timescale/timescaledb:latest-pg17
        imagePullPolicy: Always
        env:
        - name: POSTGRES_USER
          value: "postgres"
        - name: POSTGRES_PASSWORD
          value: "password" # TODO: Change this to a real secret
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        ports:
        - containerPort: 5432
        readinessProbe:
          exec:
            command:
            - pg_isready
            - -U
            - postgres
          initialDelaySeconds: 5
          periodSeconds: 10