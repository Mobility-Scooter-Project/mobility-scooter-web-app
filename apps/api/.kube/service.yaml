apiVersion: v1
kind: Service
metadata:
  name: mswa-api
  labels:
    app: mswa-api
spec:
  selector:
    app: mswa-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP