apiVersion: v1
kind: PersistentVolume
metadata:
  name: mswa-api-pv
  namespace: mswa-api
spec:
  storageClassName: csi-cinder-sc-retain
  resources:
    requests:
      storage: 10Gi
  accessModes:
  - ReadWriteOnce