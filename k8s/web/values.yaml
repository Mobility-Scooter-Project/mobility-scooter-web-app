replicas: 1
image:
  repository: ghcr.io/mobility-scooter-project/mobility-scooter-web-app/web
  tag: "latest"

ingress:
  subdomain: mswa-web

containerPort: 3000

configmap:
  data:
    NODE_TLS_REJECT_UNAUTHORIZED=0
    API_BASE_URL=http://localhost:3000
    API_KEY=your-api-key