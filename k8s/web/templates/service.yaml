apiVersion: v1
kind: Service
metadata:
    name: {{ .Release.Name }}-service
    namespace: {{ .Release.Namespace }}
    labels:
        app: {{ .Release.Name }}
spec:
    type: ClusterIP
    ports:
        - name: https
          port: {{ .Values.containerPort }}
    selector:
        app: {{ .Release.Name }}